'use strict';

// ================================================================
// CLASS: ObjetoCielo — superclase para todos los elementos visuales
//        que se dibujan dentro de la textura canvas del cielo.
//        Cada subclase implementa dibujar() con su propia lógica.
// ================================================================
export class ObjetoCielo {
    dibujar(_ctx, _W, _H, _rng) {
        // subclases implementan este método
    }
}
